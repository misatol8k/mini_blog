<h1>ブログ一覧</h1>
<p><%= notice %></p>
<table>
  <tr>
    <th>内容</th>
  </tr>
<% @mini_blogs.each do |mini_blog| %>
  <tr>
    <td><%= mini_blog.content %></td>
    <td><%= link_to "編集", edit_mini_blog_path(mini_blog.id) %></td>
    <td><%= link_to '削除', mini_blog_path(mini_blog.id), method: :delete ,data: { confirm: '本当に削除しますか？' } %></td>
  </tr>
<% end %>
</table>
<br>
<%= link_to 'ブログ新規作成画面に移動する', new_mini_blog_path %>
